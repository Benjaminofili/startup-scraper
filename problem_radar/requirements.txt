# Extra dependencies for the Problem Radar experiment only.
# Deliberately kept separate from the top-level requirements.txt, which
# is for the live scheduled scraper (scraper/main.py) - this experiment
# is not wired into that pipeline and shouldn't add weight to it.
#
# Install with: pip install -r problem_radar/requirements.txt

requests>=2.31.0
fastembed>=0.4.0   # embeddings (ONNX, no torch); falls back to TF-IDF if unavailable
scikit-learn>=1.3  # HDBSCAN clustering + TF-IDF fallback
numpy>=1.24
